export function OperationPage({ eyebrow, title, description, columns, rows, action }: { eyebrow: string; title: string; description: string; columns: string[]; rows: string[][]; action?: string }) { return <section><div className="page-heading"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{description}</p></div>{action && <button className="primary-button" type="button">＋ {action}</button>}</div><div className="toolbar"><input aria-label="ค้นหา" placeholder="ค้นหา..." /><select aria-label="ตัวกรอง"><option>ทั้งหมด</option><option>ใช้งานอยู่</option><option>ต้องตรวจสอบ</option></select><button type="button" className="secondary-button">ส่งออก CSV</button></div><div className="panel table-wrap"><table><thead><tr>{columns.map(column => <th key={column}>{column}</th>)}</tr></thead><tbody>{rows.map((row, index) => <tr key={`${row[0]}-${index}`}>{row.map((cell, cellIndex) => <td key={`${cell}-${cellIndex}`}>{cellIndex === row.length - 1 ? <span className="table-status">{cell}</span> : cell}</td>)}</tr>)}</tbody></table></div></section>; }
